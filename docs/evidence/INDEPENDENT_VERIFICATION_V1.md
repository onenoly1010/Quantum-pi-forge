# Independent Verification v1

Run `npm run verify:independent` from a checkout at the referenced commit.
The command validates committed evidence inputs and generates a read-only receipt.
It does not sign, deploy, mutate contracts, or determine an external verifier's conclusion.
