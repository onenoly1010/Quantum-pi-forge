# 0G Storage Upload Command Evidence

## Observation

A historical document named `0G_STORAGE_UPLOAD_COMMAND.md` records an intended procedure for uploading the Aristotle grant application to 0G Storage using `0g-storage-client`.

The recorded command downloads `0g-storage-client-linux-amd64`, uploads `0G_ARISTOTLE_GRANT_APPLICATION_PRODUCTION.md`, and expects a transaction hash, block number, and root hash.

## Boundary

This document records an intended upload procedure.

This document is not proof that the upload completed.

This document is not proof that a valid local 0G Storage client currently exists.

No private key should be written into this document or committed to the repository.

This is a claim, not a fact.
