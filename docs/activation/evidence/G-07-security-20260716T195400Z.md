# G-07 Security — Evidence

**Timestamp (UTC):** 2026-07-16T19:54:00Z

## Checks

| Area | Method | Result |
| --- | --- | --- |
| Tracked secrets (`.env`, keys, pem) | `git ls-files` filter | **None matched** |
| Process env private keys | Inspect `PRIVATE_KEY`, `DEPLOYER_PRIVATE_KEY`, `MNEMONIC`, `SEED` | **All absent** in agent environment |
| Dependency vulns | `npm audit --json` | **total 0** (info/low/moderate/high/critical all 0) |
| Wallet preflight | G-04 | Non-executing PASS; no signing |
| Contract ownership | G-05 `owner()` | **Residual: owner = untrusted wallet `0x335651…`** |
| Dual deployment sets | G-05 | **Residual: canon ambiguity** |
| CSP / `_headers` | Inspect `deploy/_headers` / `out/_headers` | Present as build inputs; full CSP content review not exhaustive this gate |
| XSS/CSRF deep scan | Not run as dedicated suite | **Residual** — not fabricated PASS |

## Gate decision

**PASS (scoped) with material residuals.**

Activation-ready security is **not** “no risks.” Material residual:

1. Contract **owner** is historically **untrusted** address.  
2. Dual on-chain address sets.  
3. No exhaustive XSS/CSRF browser suite.

These residuals **must appear in G-08** and block any claim of production-safe economic activation.
