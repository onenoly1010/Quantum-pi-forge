const fs=require("fs"),crypto=require("crypto");const p="receipts/governance/supervised-execution-gate-v1.json";const fail=m=>{console.error("FAIL supervised-execution-gate-v1: "+m);process.exit(1)};const r=JSON.parse(fs.readFileSync(p,"utf8"));if(r.status!=="sealed")fail("status not sealed");if(r.classification_only!==true)fail("classification_only must be true");for(const k of ["authorizes_execution","authorizes_wallet_actions","authorizes_private_key_access","authorizes_signing","authorizes_transaction_broadcast","authorizes_deploy","authorizes_stake","authorizes_mint","authorizes_participant_growth","private_key_present","wallet_actions","signing_attempted","transaction_broadcast","live_execution"])if(r[k]!==false)fail(k+" must be false");if((r.missing_conditions||[]).length)fail("missing conditions present");const raw=fs.readFileSync(r.doc_path,"utf8");if(crypto.createHash("sha256").update(raw).digest("hex")!==r.doc_sha256)fail("doc sha mismatch");console.log("PASS supervised-execution-gate-v1");console.log("STATUS "+r.status);console.log("NAMED_ACTION "+r.named_action);console.log("CLASSIFICATION_ONLY true");console.log("AUTHORIZES_EXECUTION false");console.log("REQUIRED_ENV_FLAG "+r.required_env_flag);
